$('.comment_area').html("<%= j(render 'public/post_comments/index', post: @post) %>");
$('.comment_count').html("<%= "コメント:" + @post.post_comments.count.to_s + "件" %>");
$('textarea').val('');

$('#comments_error').html("<%= j(render 'layouts/flash_message') %>");
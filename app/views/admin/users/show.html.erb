<h5>ユーザー詳細</h5>
  <%= image_tag @user.get_profile_image(20,20) %>
  ID<%= @user.id %>
  ユーザー名<%= @user.name %>
  自己紹介文<%= @user.introduction %>
  メールアドレス<%= @user.email %>
  アカウント状況
  <% if @user.is_stopped == true %>
  停止
  <% else %>
  有効
  <% end %>
  <%= link_to '編集する', edit_admin_user_path(@user) %>
  
  <h5>投稿(<%= @user.posts.count %>件)</h5>
  <% @user.posts.each do |post| %>
  <%= post.title %><br>
  <%= post.created_at.strftime('%Y/%m/%d') %>
  <%= link_to '詳細', admin_post_path(post.id) %>
  <% end %>